using UnityEngine;

/// <summary>
/// Marker component for the Player to allow PickupItems to find them autonomously.
/// </summary>
public class AutoPickupCollector : MonoBehaviour
{
    // Simple marker class. Used by PickupItem to find the player target.
}
